# ============================================
# PDFme Editor Configuration
# ============================================

# Database Configuration
# Railway injects this automatically when PostgreSQL plugin is attached
# DATABASE_URL=postgresql://user:password@localhost:5432/pdfme_editor

# CORS Configuration
# Set to your n8n instance domain in production
# Use '*' only for local testing
CORS_ORIGIN=*

# ============================================
# Security Configuration
# ============================================

# Editor UI Security (HTTP Basic Auth)
# Set both to enable login prompt for the editor interface
# Leave empty to disable Basic Auth (NOT recommended for production)
EDITOR_USERNAME=admin
EDITOR_PASSWORD=change_this_strong_password

# API Security (Bearer Token)
# Required for n8n API calls to /api/render and other endpoints
# This is separate from UI authentication
EDITOR_AUTH_TOKEN=your_secret_token_here

# ============================================
# Optional Features
# ============================================

# Optional: n8n Preview Webhook
# If set, /api/preview proxies requests to this n8n webhook
# N8N_PREVIEW_WEBHOOK_URL=https://your-n8n.railway.app/webhook/preview

# Server Port (optional, defaults to 3000)
# PORT=3000
