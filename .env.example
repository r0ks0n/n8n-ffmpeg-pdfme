# Railway injects DATABASE_URL when you attach a PostgreSQL plugin
# DATABASE_URL=postgresql://user:pass@host:port/db

# Optional auth for the API
EDITOR_AUTH_TOKEN=replace-me
CORS_ORIGIN=*

# Optional legacy preview proxy to n8n
# N8N_PREVIEW_WEBHOOK_URL=https://your-n8n.railway.app/webhook/preview
